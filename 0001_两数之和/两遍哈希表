class Solution(object):
    def twoSum(self, nums, target):
        """
        :type nums: List[int]
        :type target: int
        :rtype: List[int]
        """

        #两遍哈希表
        #num->index
        #O(n)

        hashmap = {}
        n = len(nums)
        for i in range(n):
            hashmap[nums[i]] = i
        for i in range(n):
            if hashmap.has_key(target - nums[i]) and i != hashmap.get(target - nums[i]):
                return [i, hashmap.get(target - nums[i])]
